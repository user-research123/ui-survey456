- 网页内容获取策略：优先使用 URL 前缀转换服务获取 Markdown，不支持时回退至 Scrapling 爬虫。已封装为 `web-content-fetcher` skill。
- 七麦数据爬虫策略：因 API 权限限制，采用有效 Cookie 配合浏览器自动化模拟滚动加载。该流程已沉淀为 `qimai-game-rank-crawler` skill。
- 用户衡初（冯洁雯）提供 Cookie 用于爬取中国区游戏免费榜与畅销榜各前 200 名数据。
- 榜单日期校准：2026-03-24 榜单数据确认可用，已修正此前关于未来日期无数据的误判。
- 七麦数据榜单抓取结果需以 Excel 格式交付，按榜单分 Sheet，包含排名、游戏名称、开发商字段。
- 交易猫 (jiaoyimao.com) 瀑布流页面反爬策略：因前端动态渲染与反爬限制，API 直连失败，需采用浏览器自动化模拟滚动加载方案。
- 闲鱼 (goofish.com) 瀑布流页面抓取策略：采用浏览器自动化配合 JS 注入滚动与 DOM 变化检测机制；分页操作需通过 `backbone` 获取元素 `ref` ID 后执行点击，直接使用 CSS Selector 点击会失败。
- 百度贴吧 (tieba.baidu.com) 瀑布流页面反爬策略：存在滑动验证码拦截，需等待自动通过或手动刷新；帖子列表难以通过简单滚动触发加载，建议优先尝试解析页面静态内容或使用特定 API。
- 微博 (weibo.com) 超话/搜索页面抓取策略：未登录状态下仅能获取部分帖子文本，作者、时间及互动数据缺失；完整数据需登录后配合浏览器自动化获取；URL 参数 `?page=N` 可用于分页导航。
- 数据交付规范：CSV 文件需添加 UTF-8 BOM 以确保 Excel 兼容，避免中文乱码；结构化数据交付优先使用 Excel 格式。
- 工具约束：`use_browser`的 `evaluate`动作必须使用 `fn`参数传递字符串化箭头函数，使用 `script`参数会触发报错。
- 百度贴吧 Cookie 注入策略：通过浏览器自动化注入完整 Cookie 串可绕过登录验证，但瀑布流加载仍受限于前端动态渲染机制，简单 JS 滚动无法触发新数据加载，需依赖 API 或分页交互。
- 百度贴吧分页机制限制：`pn`参数在大于 50 后返回数据大量重复，无法通过 HTML 分页线性获取更多新帖；目前通过多页去重仅能获取约 12-15 条有效数据。
- 百度贴吧 API 逆向工程尝试：参考 Python 代码思路尝试调用 `frs/page_pc`等内部 API，但因缺少 tbs 会话标识及复杂签名验证导致请求失败（错误码 110001），证实直接 API 调用需完整逆向工程支持。
- 微博超话评论数提取经验：评论数 DOM 结构易误读，需通过浏览器直接观察确认" 数字"或"评论"格式，避免依赖初始 extract schema。
- 游戏热度评估模型：建立包含存量 IP 基础、渠道推荐位、社交媒体声量、测试反馈及竞品对标的五维评估框架，重点关注首日 DAI 转化率及服务器承载压力。
- 《洛克王国：世界》预约量级对标：6000 万预约属现象级门槛，对标《使命召唤手游》、《绝地求生：全军出击》及《无畏契约：源能行动》，首日 DAI 预期 500-800 万。
- 《洛克王国：世界》闲鱼二手市场分析：商品主要分为虚拟道具/兑换码 (16.7%)、称号/奖牌 (13.3%)、邀请助力 (13.3%)、战令宠物 (13.3%)、代肝 (10%)、学号 (10%)、WeGame 代领 (10%) 等 10 类，低单价虚拟物品交易活跃。
- 《洛克王国：世界》微博超话诉求分布：水帖/签到 (45%)、代肝服务 (25%)、社交组队 (17.5%)、交易咨询 (7.5%)、游戏讨论 (3.75%)、其他推广 (1.25%)；近半数为无效水帖，代肝为核心交易需求。
- 钉钉消息推送配置：目标群"平台业务线 - 用研组" (ID: `cidaHhpwmWApn66IbJ3wQxUmQ==`) 已配置自定义 Webhook 机器人，Token 为 `4871c47f3c1df3c4e2d6fba0121d584616c78d17deae78aca48fba07d8556554`，安全关键词为"悟空"。
- 小红书交易监测架构：针对《洛克王国：世界》建立"双轨监测+AI 语义过滤"系统。精准轨使用高置信度关键词；发现轨使用宽泛意图词捕获新品类；每日自动运行并提取新词，默认关闭自动推送，需人工确认后发送，解决静态关键词漏检问题。
- 协作边界原则：严禁未经用户明确指令执行不可逆或干扰性操作（如自动发送钉钉消息）。所有钉钉推送任务默认处于静默模式，仅在用户显式要求时触发。
- 调研自动化技能：已创建 `auto-research` skill (QoderWork 版)，支持并行搜索、实时进度追踪及钉钉文档发布，耗时约 8-10 分钟。
- 谷子市场研究结论：2025 年市场规模 2021 亿元，核心痛点为质量监管缺失与二手炒作；切入点包括品质认证平台、IP 孵化服务及数字化创新。
- 《王者荣耀世界》全链路自动化监控体系：建立从数据抓取（官网活动/螃蟹账号/盼之/闲鱼/微博）、JSON 数据源汇总、HTML 报告生成到 GitHub Pages 自动推送的日报闭环。每日 15:30 自动生成总结并发布。
- 《王者荣耀世界》报告数据规范：官方事件仅收录官网"活动"Tab 内容；竞品动态需分平台（螃蟹/盼之/闲鱼）总结；总结板块独立于 `summary.json`，综合三方维度。
- 官网监控 Tab 隔离与清洗原则：定时任务必须严格验证"活动"Tab 激活状态，剔除"新闻"等非活动信息，确保数据纯净度。
- 用户需求追踪数据源：小红书舆情热点（如 ID 抢注攻略、极品 ID 稀缺性讨论、公测时间传播等）需每日人工或 AI 识别后汇入 `user_feedback.json`。
- 微博舆情分类规则：定义 9 类标签（游戏攻略、社交互动、代练代肝、道具交易、游戏内容、游戏技术、同人创作、游戏资讯、水贴），用于标准化分析玩家诉求分布。
- UI 调研问卷实施策略：采用极简双板块设计（上方垂直展示 Figma 原型预览，下方嵌入问卷 iframe 供用户填写提交）；问卷平台优先选用阿里系服务（如交易猫 survey.jiaoyimao.com）。
- 历史数据完整性原则：在进行 UI 重构或样式更新时，必须确保原有 HTML 结构中所有历史日期的内容区块完整保留，避免因合并冲突导致数据丢失。
- Git 合并策略：当本地与远程仓库存在大量冲突时，使用 `git merge origin/main -X theirs` 优先保留远程最新代码，然后手动验证并修复特定问题。
- GitHub Pages 缓存处理：GitHub Pages 使用 CDN 分发，更新后有 1-5 分钟延迟；若页面显示旧版本，需强制刷新（macOS: `Cmd+Shift+R`，Windows: `Ctrl+Shift+R`）或在 URL 后加参数绕过缓存。
- GitHub Pages 部署故障排查：若访问返回 404 且仓库为空，通常是因为本地提交尚未推送到远程；需执行 `git push` 完成同步，推送成功后 1-5 分钟内自动部署。
- 交易猫新游品类机会挖掘策略：引入 AI 构建"前置挖掘+后置追踪"体系。上线前通过多平台舆情分析提前识别高潜力交易品类，实现资源前置配置；上线后实时追踪榜单表现与市场热度，动态评估并调整资源投入，解决传统依赖用户反馈导致的滞后性问题。
- 营销文案润色偏好：用户倾向于将营销分析内容结构化呈现，分为"线上/线下"双维度，并附加策略总结，语言风格需专业精炼且具有商业洞察力。
- 螃蟹账号平台 (pxb7.com) 抓取策略：因反爬拦截风险，采用浏览器自动化注入 Cookie 后模拟滚动加载；需处理"最新发布"排序切换及动态商品列表提取。
- 盼之代售平台 (pzds.com) 抓取策略：采用浏览器自动化配合 JS 注入滚动加载；需多次滚动并等待内容渲染以突破单次加载数量限制，确保获取足量商品数据（如 100 条）。
- HTML 报告初始化逻辑维护：新增日期内容区块时，必须同步更新 `initDateTabs()` 函数中的默认激活 ID，否则会导致按钮状态与实际显示内容不一致。
- 《王者荣耀世界》日报脚本数据兼容性：处理竞品数据时需兼容 `price_ranges` 的不同结构（螃蟹为字典含 count/percentage，盼之为整数值）及字段名差异（`total` vs `total_count`）。
- 《王者荣耀世界》HTML 报告更新策略：正则替换需精确匹配 `<div class="section-content">` 内部内容，避免破坏外层 `<div class="content">` 结构或误删注释标记。

## UI 调研问卷页面设计规范
UI 调研问卷实施策略：采用极简双板块设计（上方垂直展示 Figma 原型预览，下方嵌入问卷 iframe 供用户填写提交）；问卷平台优先选用阿里系服务（如交易猫 survey.jiaoyimao.com）。

**实施细节**：
- **原型嵌入**：使用 `<iframe>` 直接嵌入 Figma 原型链接，设置合适高度（如 600px）并允许滚动交互。若遇加载失败，需提供备用跳转链接。
- **问卷嵌入**：使用 `<iframe>` 直接嵌入阿里系问卷平台（如 survey.jiaoyimao.com），确保用户无需跳转即可填写。

## 王者荣耀UI调研重点
《王者荣耀》UI调研核心关注点：战斗HUD的信息即时性（血量、冷却、小地图）、技能按钮布局的精准度与防误触、局外系统（商城/英雄详情）的视觉冲击力与信息查阅效率的平衡。

## 鸣潮UI调研重点
《鸣潮》UI调研核心关注点：开放世界探索的沉浸感（弱引导/智能动态指引）、高速战斗中的声骸/共鸣技能反馈清晰度、菜单系统（声骸/武器管理）的浏览效率与3D展示偏好。

## UI问卷设计原则
UI调研问卷需结合游戏类型特性设计：MOBA类侧重竞技信息获取效率与操作手感；开放世界动作类侧重视觉沉浸感、探索引导的自然度及复杂系统（如声骸）的管理便捷性。

## GitHub Pages 部署与仓库管理
项目代码托管于 GitHub 仓库。若需更换仓库，应先移除旧 `origin` (`git remote remove origin`)，再添加新地址 (`git remote add origin <new_url>`)。推送前确保已进入包含 `.git` 目录的项目根路径，否则报错 `fatal: not a git repository`。

**Pages 配置要点**：
- 需在仓库 Settings > Pages 中手动开启，选择 Deploy from a branch (main/root)。
- 默认主页文件名为 `index.html`。若使用其他文件名（如 `ui-survey.html`），访问时需指定完整路径。

## Figma 嵌入权限限制
Figma 原型嵌入权限限制：若 Figma 文件未设置为“Anyone with the link can view”，iframe 嵌入将因登录要求而加载失败（显示空白或登录提示）；需确保原型文件公开访问或提供备用跳转链接。

## 游戏账号交易UI调研核心维度
《王者荣耀》等账号交易UI调研需聚焦四大维度：1. 安全与基础（实名状态、区服/登录方式）；2. 核心资产（贵族等级、段位、高价值皮肤总量如典藏/无双）；3. 特色亮点（特定英雄全皮/稀有皮肤、账号定位标签）；4. 视觉呈现（皮肤预览图占比与信息浮层的平衡）。建议采用“悬停浮层”展示核心硬指标，以兼顾视觉冲击与决策效率。

## Figma 海外资源加速方案
针对嵌入的 Figma 原型（海外服务器）加载慢问题，推荐组合方案：
1. **短期优化**：实施懒加载（Lazy Loading），用户点击“查看方案”时才动态加载 iframe；配合 DNS 预解析 (`dns-prefetch`) 和预连接 (`preconnect`)。
2. **中长期优化**：预渲染静态截图作为占位符，点击后加载完整交互；或迁移至国内设计平台（如蓝湖、即时设计、MasterGo）以彻底解决跨境访问延迟。

## UI 调研问卷实施策略
采用极简双板块设计（上方垂直展示原型预览，下方嵌入问卷 iframe）；问卷平台优先选用阿里系服务（如交易猫 survey.jiaoyimao.com）。

**实施细节**：
- **原型嵌入**：使用 `<iframe>` 直接嵌入原型链接（Figma/MasterGo），设置合适高度并允许滚动交互。若遇加载失败，需提供备用跳转链接。
- **问卷嵌入**：使用 `<iframe>` 直接嵌入阿里系问卷平台，确保用户无需跳转即可填写。

## Figma/MasterGo 嵌入与加速策略
针对海外设计平台（Figma）加载慢或权限受限问题，优先迁移至国内平台（如 MasterGo）；若需嵌入，确保文件设为“Anyone with the link can view”或开启允许嵌入；推荐组合使用懒加载、DNS 预解析及静态截图占位符以提升首屏性能。

## MasterGo 嵌入链接规范
UI 调研问卷中 MasterGo 原型嵌入链接应使用基础格式：`https://mastergo.com/prototype/{id}?ht=1&zs=1&pageId=M&layerId={layer_id}`，避免携带 `&shared=true` 等冗余参数以确保最佳兼容性。

## GitHub Pages 静态托管更新规范
GitHub Pages 为静态托管服务，无法直接远程修改文件。更新流程需本地化：克隆仓库 -> 编辑 HTML（插入新日期区块、同步更新导航按钮 active 状态及页面总结摘要）-> 提交并推送 (`git push`)。若遇冲突，优先使用 `git pull --rebase` 保持提交历史整洁。

## 异环市场监控日报网页结构规范
《异环》市场监控日报网页结构规范：采用五板块布局（总结/App下载图/官方事件时间轴/竞品动态追踪/用户需求追踪）。竞品与需求模块需实现日期按钮筛选功能，日期从新到旧排序，默认显示最新日期，点击按钮时通过JS类切换仅渲染对应日期内容，确保多模块（螃蟹/盼之/闲鱼）同步更新。

## UI 调研问卷页面布局优化策略
UI 调研问卷页面 (ui-survey123) 布局优化：将方案 A/B/C/D 的呈现宽度统一收窄至 800px 并居中显示，两侧留出更多空白区域以优化用户上下滑动体验。

## 盼之商品类型筛选栏选项
盼之平台 (pzds.com) 王者荣耀世界商品列表页顶部“商品类型”筛选栏包含固定选项：成品号、昵称 (hot)、代肝 (hot)、充值 (new)、捏脸。抓取时需识别这些特定文本以确认页面加载正确性。
